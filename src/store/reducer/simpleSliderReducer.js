import * as actionTypes from '../action/actionTypes';

const initialState = {
    
}

const simpleSliderReducer = ( state = initialState, action) => {
    switch(action.type){
        case :
        
    }
}